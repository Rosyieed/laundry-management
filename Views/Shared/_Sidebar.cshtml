@{
    var controller = ViewContext.RouteData.Values["controller"]?.ToString();
    var action = ViewContext.RouteData.Values["action"]?.ToString();
}

@section Styles {
    <style>
      .logo-container {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            border-radius: 50%;
            width: 100px;
            height: 100px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 2rem;
            border: 1px solid rgba(255, 255, 255, 0.2);
        }
    </style>
}

<aside class="app-sidebar bg-body-secondary shadow" data-bs-theme="dark">
        <!--begin::Sidebar Brand-->
        <div class="sidebar-brand">
          <!--begin::Brand Link-->
          <a asp-controller="Home" asp-action="Index" class="brand-link">
            <!--begin::Brand Image-->
            <div class="logo-container">
                <i class="fa-solid fa-shirt" style="color: white;"></i>
            </div>
            <!--end::Brand Image-->
            <!--begin::Brand Text-->
            <span class="brand-text fw-light">Laundry Management</span>
            <!--end::Brand Text-->
          </a>
          <!--end::Brand Link-->
        </div>
        <!--end::Sidebar Brand-->
        <!--begin::Sidebar Wrapper-->
        <div class="sidebar-wrapper">
          <nav class="mt-2">
            <!--begin::Sidebar Menu-->
            <ul
              class="nav sidebar-menu flex-column"
              data-lte-toggle="treeview"
              role="navigation"
              aria-label="Main navigation"
              data-accordion="false"
              id="navigation"
            >
              <li class="nav-header">MASTER</li>
              @if (User.IsInRole("Owner"))
              {
                  <li class="nav-item">
                     <a asp-controller="MasterUser"
                        asp-action="Index"
                        class="nav-link @(controller == "MasterUser" ? "active" : "")">
                        <i class="nav-icon fa-solid fa-users"></i>
                        <p>User Management</p>
                    </a>
                  </li>
              }
              @if (User.IsInRole("Owner") || User.IsInRole("Admin"))
              {
                  <li class="nav-item">
                     <a asp-controller="MasterJasa"
                        asp-action="Index"
                        class="nav-link @(controller == "MasterJasa" ? "active" : "")">
                        <i class="nav-icon fa-solid fa-boxes-stacked"></i>
                        <p>Jasa Management</p>
                    </a>
                  </li>
              }
            </ul>
            <!--end::Sidebar Menu-->
          </nav>
        </div>
        <!--end::Sidebar Wrapper-->
      </aside>